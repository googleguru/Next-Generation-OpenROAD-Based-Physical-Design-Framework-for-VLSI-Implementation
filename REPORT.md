# Physical Design Experiment Report

> **Note:** This file is auto-generated by `make report`.
> Numeric results will appear here only after benchmark runs complete
> with valid PDK and design collateral.  No results have been fabricated.

---

## 1. Framework Overview

The Next-Generation Physical Design Framework extends a standard EDA flow with:

- **Adaptive Flow Controller** — rule-based + optional Bayesian parameter tuning
- **Cross-Stage QoR Feedback Graph** — placement signals inform CTS/routing
- **Failure Diagnostics Engine** — automatic classification, root-cause, and remediation
- **Structured Experiment Manager** — YAML-driven, seeded, reproducible sweeps
- **Publication-Ready Reporting** — white-background figures, auto-updated README

---

## 2. Benchmark Status

### IPSD Benchmarks

| Circuit | Status | Reason if Skipped |
|---------|--------|-------------------|
| gcd | Awaiting collateral | PDK/netlist not placed |
| aes | Awaiting collateral | PDK/netlist not placed |
| ibex | Awaiting collateral | PDK/netlist not placed |
| jpeg_encoder | Awaiting collateral | PDK/netlist not placed |

### ISCAS Benchmarks

| Circuit | Type | Status |
|---------|------|--------|
| c17–c7552 | Combinational | SKIPPED (no clock domain) |
| s27, s298, s526 | Sequential | Awaiting Yosys synthesis |
| s1423, s5378, s38584 | Sequential | Awaiting Yosys synthesis |

---

## 3. QoR Results

*Populated automatically after `make sweep` completes.*

Metrics collected per run:
- **WNS** (ns) — Worst Negative Slack (setup)
- **TNS** (ns) — Total Negative Slack
- **Overflow** (%) — Placement RUDY overflow proxy
- **DRC Violations** — count of design-rule violations
- **Routed Wirelength** (μm)
- **Utilization** (%) — core cell area utilization
- **Total Runtime** (s)
- **Retries / Rollbacks** — per run

---

## 4. Ablation Study

*Populated automatically after `make ablation` completes.*

Planned ablation configurations:

| Config | Description |
|--------|-------------|
| `baseline` | Standard flow, no extra modules |
| `+experiment_manager` | + structured experiment tracking |
| `+adaptive_tuning` | + rule-based parameter adaptation |
| `+diagnostics` | + automatic failure diagnosis |
| `full` | All modules enabled |

---

## 5. Figures

*Generated figures will appear in `outputs/figures/` after `make report`.*

Expected figure set:
- `wns_tns_comparison.png`
- `overflow_trend.png`
- `congestion_vs_timing.png`
- `runtime_breakdown.png`
- `ablation.png`
- `benchmark_summary.png`
- `multi_seed_box_wns.png`
- `success_rate.png`

---

## 6. Reproducibility

Every run stores a manifest in `outputs/runs/<run_id>_summary.json` containing:
- Exact parameter values used at each stage
- Git SHA of the framework
- Seed value
- Runtime per stage
- All extracted metrics

Re-run with identical seed and config to reproduce results deterministically.

---

## 7. Assumptions and Limitations

- All results depend on publicly available PDK collateral placed by the user.
- ISCAS-85 (combinational) circuits are excluded from physical implementation.
- Timing values are post-layout estimates using parasitic RC estimation;
  sign-off requires full RC extraction.
- DRC/antenna results require a complete PDK DRC rule deck.
- GDSII output is disabled by default; enable via `write_gds: true` in config.

---

*Auto-generated by the Physical Design Framework — update with `make report`*
